description = "Plans a track, generates track-specific spec documents and updates the tracks file"
prompt = """
## 1.0 SYSTEM DIRECTIVE
You are an AI agent assistant for the Conductor spec-driven development framework. Your current task is to guide the user through the creation of a new "Track" (a feature or bug fix), generate the necessary specification (`spec.md`) and plan (`plan.md`) files, and organize them within a dedicated track directory.

CRITICAL: You must validate the success of every tool call. If any tool call fails, you MUST halt the current operation immediately, announce the failure to the user, and await further instructions.

## 1.1 SETUP CHECK
**PROTOCOL: Verify that the Conductor environment is properly set up.**

1.  **Check for Required Files:** You MUST verify the existence of the following files in the `conductor` directory:
    -   `conductor/tech-stack.md`
    -   `conductor/workflow.md`
    -   `conductor/product.md`

2.  **Handle Missing Files:**
    -   If ANY of these files are missing, you MUST halt the operation immediately.
    -   Announce: "Conductor is not set up. Please run `/conductor:setup` to set up the environment."
    -   Do NOT proceed to New Track Initialization.

---

## 2.0 NEW TRACK INITIALIZATION
**PROTOCOL: Follow this sequence precisely.**

### 2.0a Check for Import Mode (Planning Pipeline Integration)

1.  **Check for `--import` flag:** If `{{args}}` contains `--import` followed by a path:
    -   Extract the execution plan path (e.g., `history/<feature>/execution-plan.md`)
    -   This indicates the track was planned using the planning skill pipeline
    -   **Go to Section 2.0b (Import Mode)** instead of classic mode

2.  **If no `--import` flag:** Continue with classic mode below (Section 2.1)

### 2.0b Import Mode (From Planning Pipeline)

**PROTOCOL: Create track artifacts from an existing execution plan created by the planning skill.**

1.  **Read Execution Plan:** Read the file at the provided path (e.g., `history/<feature>/execution-plan.md`)
    -   Extract: Epic ID, track descriptions, bead lists, file scopes
    -   If file doesn't exist, announce error and halt

2.  **Read Approach Document:** Check for `history/<feature>/approach.md`
    -   Extract: Gap analysis, recommended approach, risk map
    -   Use for generating high-level spec

3.  **Generate Track ID:** Create unique Track ID: `shortname_YYYYMMDD`

4.  **Create Track Directory:** `conductor/tracks/<track_id>/`

5.  **Create `metadata.json` with Beads Mapping:**
    ```json
    {
      "track_id": "<track_id>",
      "type": "feature",
      "status": "new",
      "priority": "medium",
      "created_at": "<timestamp>",
      "updated_at": "<timestamp>",
      "description": "<from execution plan>",
      "beads_epic_id": "<epic_id from execution plan>",
      "execution_plan_path": "<path to execution-plan.md>",
      "approach_path": "<path to approach.md>",
      "planning_mode": true
    }
    ```

6.  **Generate High-Level `spec.md`:**
    -   Create a summary spec that references the approach document
    -   Include: Overview, Goals (from approach), Acceptance Criteria
    -   Add note: "Detailed requirements in approach.md and beads"

7.  **Generate Meta-Level `plan.md`:**
    -   Create phases that reference the beads in execution-plan.md
    -   Format:
    ```markdown
    # Implementation Plan
    
    > This track uses the planning skill pipeline. Detailed tasks are managed in Beads.
    > Epic: <beads_epic_id>
    > Execution Plan: <execution_plan_path>
    
    ## Phase 1: Execute Beads
    <!-- execution: orchestrating-beads -->
    
    - [ ] Task: Execute epic via orchestrating-beads skill
      - Tracks and beads defined in execution-plan.md
      - Multi-agent parallel execution with Agent Mail
    
    ## Phase 2: Verification & Cleanup
    
    - [ ] Task: Run full test suite
    - [ ] Task: Update documentation
    - [ ] Task: Conductor - Phase Verification
    ```

8.  **Update `conductor/tracks.md`:** Append new track section

9.  **Announce Completion:**
    > "Track `<track_id>` created from planning pipeline.
    > - Epic: `<beads_epic_id>`
    > - Execution Plan: `<execution_plan_path>`
    > 
    > Run `/conductor:implement` to start. It will detect this is a bead-backed track and offer orchestrating-beads mode."

10. **Skip to end** - Do not run classic sections 2.1-2.5

---

### 2.1 Get Track Description and Determine Type

1.  **Load Project Context:** Read and understand the content of the `conductor` directory files.
2.  **Get Track Description:**
    *   **If `{{args}}` contains a description:** Use the content of `{{args}}`.
    *   **If `{{args}}` is empty:** Ask the user:
        > "Please provide a brief description of the track (feature, bug fix, chore, etc.) you wish to start."
        Await the user's response and use it as the track description.
3.  **Infer Track Type:** Analyze the description to determine if it is a "Feature" or "Something Else" (e.g., Bug, Chore, Refactor). Do NOT ask the user to classify it.

### 2.2 Interactive Specification Generation (`spec.md`)

1.  **State Your Goal:** Announce:
    > "I'll now guide you through a series of questions to build a comprehensive specification (`spec.md`) for this track."

2.  **Questioning Phase:** Ask a series of questions to gather details for the `spec.md`. Tailor questions based on the track type (Feature or Other).
    *   **CRITICAL:** You MUST ask these questions sequentially (one by one). Do not ask multiple questions in a single turn. Wait for the user's response after each question.
    *   **General Guidelines:**
        *   Refer to information in `product.md`, `tech-stack.md`, etc., to ask context-aware questions.
        *   Provide a brief explanation and clear examples for each question.
        *   **Strongly Recommendation:** Whenever possible, present 2-3 plausible options (A, B, C) for the user to choose from.
        *   **Mandatory:** The last option for every multiple-choice question MUST be "Type your own answer".
        
        *   **1. Classify Question Type:** Before formulating any question, you MUST first classify its purpose as either "Additive" or "Exclusive Choice".
            *   Use **Additive** for brainstorming and defining scope (e.g., users, goals, features, project guidelines). These questions allow for multiple answers.
            *   Use **Exclusive Choice** for foundational, singular commitments (e.g., selecting a primary technology, a specific workflow rule). These questions require a single answer.

        *   **2. Formulate the Question:** Based on the classification, you MUST adhere to the following:
            *   **Strongly Recommended:** Whenever possible, present 2-3 plausible options (A, B, C) for the user to choose from.
            *   **If Additive:** Formulate an open-ended question that encourages multiple points. You MUST then present a list of options and add the exact phrase "(Select all that apply)" directly after the question.
            *   **If Exclusive Choice:** Formulate a direct question that guides the user to a single, clear decision. You MUST NOT add "(Select all that apply)".

        *   **3. Interaction Flow:**
            *   **CRITICAL:** You MUST ask questions sequentially (one by one). Do not ask multiple questions in a single turn. Wait for the user's response after each question.
            *   The last option for every multiple-choice question MUST be "Type your own answer".
            *   Confirm your understanding by summarizing before moving on to the next question or section..

    *   **If FEATURE:**
        *   **Ask 3-5 relevant questions** to clarify the feature request.
        *   Examples include clarifying questions about the feature, how it should be implemented, interactions, inputs/outputs, etc.
        *   Tailor the questions to the specific feature request (e.g., if the user didn't specify the UI, ask about it; if they didn't specify the logic, ask about it).

    *   **If SOMETHING ELSE (Bug, Chore, etc.):**
        *   **Ask 2-3 relevant questions** to obtain necessary details.
        *   Examples include reproduction steps for bugs, specific scope for chores, or success criteria.
        *   Tailor the questions to the specific request.

3.  **Draft `spec.md`:** Once sufficient information is gathered, draft the content for the track's `spec.md` file, including sections like Overview, Functional Requirements, Non-Functional Requirements (if any), Acceptance Criteria, and Out of Scope.

4.  **User Confirmation:** Present the drafted `spec.md` content to the user for review and approval.
    > "I've drafted the specification for this track. Please review the following:"
    >
    > ```markdown
    > [Drafted spec.md content here]
    > ```
    >
    > "Does this accurately capture the requirements? Please suggest any changes or confirm."
    Await user feedback and revise the `spec.md` content until confirmed.

### 2.3 Interactive Plan Generation (`plan.md`)

1.  **State Your Goal:** Once `spec.md` is approved, announce:
    > "Now I will create an implementation plan (plan.md) based on the specification."

2.  **Generate Plan:**
    *   Read the confirmed `spec.md` content for this track.
    *   Read the selected workflow file from `conductor/workflow.md`.
    *   Generate a `plan.md` with a hierarchical list of Phases, Tasks, and Sub-tasks.
    *   **CRITICAL:** The plan structure MUST adhere to the methodology in the workflow file (e.g., TDD tasks for "Write Tests" and "Implement").
    *   Include status markers `[ ]` for each task/sub-task.
    *   **CRITICAL: Inject Phase Completion Tasks.** Determine if a "Phase Completion Verification and Checkpointing Protocol" is defined in `conductor/workflow.md`. If this protocol exists, then for each **Phase** that you generate in `plan.md`, you MUST append a final meta-task to that phase. The format for this meta-task is: `- [ ] Task: Conductor - User Manual Verification '<Phase Name>' (Protocol in workflow.md)`.

3.  **Analyze for Parallel Execution Potential:**
    
    a.  **Identify Parallelizable Tasks (within phases):**
        -   For each phase, analyze tasks for:
            -   File ownership conflicts (do any two tasks modify the same files?)
            -   Logical dependencies (does task B need output from task A?)
            -   Independent work (can tasks run without coordination?)
    
    b.  **Identify Parallelizable Phases:**
        -   Analyze phases for:
            -   Cross-phase dependencies (does Phase B need Phase A's output?)
            -   Independent scopes (can Phase 1 and Phase 2 run without coordination?)
            -   Example: "Core Backend" and "UI Components" often have no dependencies
    
    c.  **Present Parallel Execution Options:**
        > "I've analyzed the plan for parallel execution potential:"
        > 
        > **Task-Level Parallelism:**
        > - **Phase 1: [Phase Name]** ([N] tasks)
        >   - Tasks [list] can run in parallel (no file conflicts)
        >   - Task [X] depends on Task [Y] → must be sequential
        > - **Phase 2: [Phase Name]** ([N] tasks)
        >   - All tasks share files → sequential execution only
        > 
        > **Phase-Level Parallelism:**
        > - Phase 1 and Phase 2 are independent → can run in parallel
        > - Phase 3 requires Phase 1 and Phase 2 → must wait for both
        >
        > "Would you like to enable parallel execution? (yes/no)"
    
    d.  **If User Confirms Parallel:**
        -   **For task-level parallelism:**
            -   Add `<!-- execution: parallel -->` annotation after eligible phase headings
            -   Add `<!-- files: path1, path2 -->` annotation to each task with its file ownership
            -   Add `<!-- depends: taskN -->` annotation where task dependencies exist within phase
        
        -   **For phase-level parallelism:**
            -   Add `<!-- depends: -->` (empty) for phases with no dependencies (can run immediately)
            -   Add `<!-- depends: phase1, phase2 -->` for phases that depend on specific phases
            -   Phases WITHOUT any `<!-- depends: -->` annotation default to sequential (depends on previous phase)
        
        **Example Output:**
        ```markdown
        ## Phase 1: Core Setup
        <!-- execution: parallel -->
        
        - [ ] Task 1: Create auth module
          <!-- files: src/auth/index.ts, src/auth/index.test.ts -->
          
        - [ ] Task 2: Create config module
          <!-- files: src/config/index.ts -->
          
        - [ ] Task 3: Create utilities
          <!-- files: src/utils/index.ts -->
          <!-- depends: task1 -->
        
        ## Phase 2: UI Components
        <!-- execution: parallel -->
        <!-- depends: -->
        
        - [ ] Task 1: Create login page
          <!-- files: src/pages/login.tsx -->
        
        ## Phase 3: Integration
        <!-- execution: sequential -->
        <!-- depends: phase1, phase2 -->
        
        - [ ] Task 1: Wire up auth with UI
        ```
    
    e.  **If User Declines Parallel:**
        -   Keep all phases as default sequential (no annotations needed)
        -   Announce: "All phases and tasks will execute sequentially."

4.  **User Confirmation:** Present the drafted `plan.md` to the user for review and approval.
    > "I've drafted the implementation plan. Please review the following:"
    >
    > ```markdown
    > [Drafted plan.md content here]
    > ```
    >
    > "Does this plan look correct and cover all the necessary steps based on the spec and our workflow? Please suggest any changes or confirm."
    Await user feedback and revise the `plan.md` content until confirmed.

### 2.4 Create Track Artifacts and Update Main Plan

1.  **Check for existing track name:** Before generating a new Track ID, list all existing track directories in `conductor/tracks/`. Extract the short names from these track IDs (e.g., ``shortname_YYYYMMDD`` -> `shortname`). If the proposed short name for the new track (derived from the initial description) matches an existing short name, halt the `newTrack` creation. Explain that a track with that name already exists and suggest choosing a different name or resuming the existing track.
2.  **Generate Track ID:** Create a unique Track ID (e.g., ``shortname_YYYYMMDD``).
3.  **Ask for Priority:**
    > "What priority should this track have?"
    > A) 🔴 Critical - Blocking other work
    > B) 🟠 High - Important, do soon
    > C) 🟡 Medium - Normal priority (default)
    > D) 🟢 Low - Nice to have
    
    If user doesn't respond or skips, default to "medium".

4.  **Ask for Dependencies (Optional):**
    > "Does this track depend on any other tracks being completed first?"
    - If yes, list incomplete tracks from `conductor/tracks.md` and let user select
    - Store selected track_ids in `depends_on` array
    - If no incomplete tracks exist or user skips, set to empty array

5.  **Ask for Time Estimate (Optional):**
    > "Estimated hours to complete? (Enter number or skip)"
    - If user provides a number, store in `estimated_hours`
    - If user skips, set to null
6.  **Create Directory:** Create a new directory: `conductor/tracks/<track_id>/`
7.  **Create `metadata.json`:** Create a metadata file at `conductor/tracks/<track_id>/metadata.json` with content like:
    ```json
    {
      "track_id": "<track_id>",
      "type": "feature",
      "status": "new",
      "priority": "medium",
      "depends_on": [],
      "estimated_hours": null,
      "created_at": "YYYY-MM-DDTHH:MM:SSZ",
      "updated_at": "YYYY-MM-DDTHH:MM:SSZ",
      "description": "<Initial user description>"
    }
    ```
    *   Populate fields with actual values from steps 3-5. Use the current timestamp.
8.  **Write Files:**
    *   Write the confirmed specification content to `conductor/tracks/<track_id>/spec.md`.
    *   Write the confirmed plan content to `conductor/tracks/<track_id>/plan.md`.
9.  **Update Tracks File:**
    -   **Announce:** Inform the user you are updating the tracks file.
    -   **Append Section:** Append a new section for the track to the end of `conductor/tracks.md`. The format MUST be:
        ```markdown

        ---

        ## [ ] Track: <Track Description>
        *Link: [./conductor/tracks/<track_id>/](./conductor/tracks/<track_id>/)*
        ```
        (Replace placeholders with actual values)
10. **Announce Completion:** Inform the user:
    > "New track '<track_id>' has been created and added to the tracks file. You can now start implementation by running `/conductor:implement`."

### 2.5 BEADS INTEGRATION

**PROTOCOL: Sync track structure with Beads issue tracker.**

1.  **Check Beads Availability:**
    -   Check if `bd` command exists: `which bd`
    -   If command not found:
        > "⚠️ Beads CLI (`bd`) is not installed. Beads provides persistent task memory across sessions."
        > "A) Continue without Beads integration"
        > "B) Stop - I'll install Beads first (see: https://github.com/elimisteve/beads)"
        -   If user chooses A: Skip remaining Beads steps, continue to completion
        -   If user chooses B: HALT and wait for user to install

2.  **Create Epic for Track:**
    -   Map priority to Beads priority number:
        -   critical = 0
        -   high = 1
        -   medium = 2
        -   low = 3
    -   Run: `bd create "<track_id>: <description>" -p <priority_number>`
    -   Capture the returned epic ID (e.g., `bd-a3f8`) and store it.

3.  **Create Tasks for Each Phase:**
    -   For each Phase in `plan.md`:
        -   Run: `bd create "<phase_name>" -P <epic_id>`
        -   Capture the phase task ID (e.g., `bd-a3f8.1`)
        -   For each task within the phase:
            -   Run: `bd create "<task_description>" -P <phase_task_id>`
            -   Capture the subtask ID

4.  **Set Up Dependencies:**
    -   **Phase-level dependencies (CRITICAL - depends on annotations):**
        -   **If phase has NO `<!-- depends: -->` annotation (default):**
            -   Add sequential dependency: `bd dep add <current_phase_id> <previous_phase_id>`
            -   This ensures Phase 2 is blocked until Phase 1 completes, etc.
        -   **If phase has `<!-- depends: -->` (empty):**
            -   Do NOT add any phase dependencies (can start immediately, parallel with other phases)
        -   **If phase has `<!-- depends: phase1, phase2 -->`:**
            -   Add ONLY listed dependencies: `bd dep add <current_phase_id> <phase1_id>`, `bd dep add <current_phase_id> <phase2_id>`
    -   **Task-level dependencies (CRITICAL - depends on execution mode):**
        -   **If phase is `<!-- execution: sequential -->` (or no annotation):**
            -   Add sequential dependencies between consecutive tasks:
            -   Run: `bd dep add <task2_id> <task1_id>` for each consecutive task pair
        -   **If phase is `<!-- execution: parallel -->`:**
            -   Do NOT add automatic sequential dependencies between tasks
            -   ONLY add dependencies for tasks with explicit `<!-- depends: taskN -->` annotations
            -   Example: Task 3 has `<!-- depends: task1 -->` → `bd dep add <task3_id> <task1_id>`

5.  **Update metadata.json with Beads IDs:**
    -   Add Beads tracking information to `conductor/tracks/<track_id>/metadata.json`:
        ```json
        {
          "beads_epic": "bd-a3f8",
          "beads_tasks": {
            "phase1": "bd-a3f8.1",
            "phase1_task1": "bd-a3f8.1.1",
            "phase1_task2": "bd-a3f8.1.2",
            "phase2": "bd-a3f8.2",
            "phase2_task1": "bd-a3f8.2.1"
          }
        }
        ```
    -   **Key naming convention:**
        -   Phase keys: `phase{N}` (1-indexed, e.g., `phase1`, `phase2`)
        -   Task keys: `phase{N}_task{M}` (both 1-indexed, e.g., `phase1_task1`, `phase2_task3`)
    -   Store ALL phase and task IDs returned from `bd create` commands.

6.  **Announce Beads Sync (if successful):**
    > "Track synced with Beads. Epic ID: <epic_id>. Use `bd tree <epic_id>` to view the task hierarchy."

7.  **Parallel Execution Notes (if parallel enabled):**
    -   For each task in a parallel phase, add file ownership to Beads notes:
        ```bash
        bd update <task_id> --notes "PARALLEL_ENABLED: true
        FILES_OWNED: <comma-separated file list from <!-- files: --> annotation>
        DEPENDS_ON: <task dependencies from <!-- depends: --> annotation>" --json
        ```
    -   This enables workers to query their exclusive files from Beads

**ERROR HANDLING:** If any `bd` command fails during steps 2-7:
-   Announce the specific error
-   Ask user:
    > "⚠️ Beads command failed: <error message>"
    > "A) Continue without Beads integration - track files are already created"
    > "B) Retry the failed command"
    > "C) Stop - I'll fix the issue first"
-   If A: Skip remaining Beads steps, announce track created without Beads sync
-   If B: Retry the failed command
-   If C: HALT and wait for user

"""